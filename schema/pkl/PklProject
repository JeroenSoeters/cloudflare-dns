amends "pkl:Project"

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.84.0"
  }
}

package {
  name = "cloudflare-dns"
  baseUri = "package://hub.platform.engineering/plugins/cloudflare-dns/schema/pkl/\(name)/\(name)"
  version = read("VERSION").text.trim()
  packageZipUrl = "https://hub.platform.engineering/plugins/cloudflare-dns/schema/pkl/\(name)/\(name)@\(version).zip"
}
