amends "pkl:Project"

// Package configuration for local development.
// Update baseUri and packageZipUrl when publishing to a registry.
package {
  name = "cloudflare-dns"
  baseUri = "package://localhost/plugins/cloudflare-dns/schema/pkl/cloudflare-dns"
  version = "0.1.0"
  packageZipUrl = "https://localhost/plugins/cloudflare-dns/schema/pkl/cloudflare-dns@\(version).zip"
}

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.80.0"
  }
}
